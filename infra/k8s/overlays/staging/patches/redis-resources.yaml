apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: redis
  namespace: taskflow-app
spec:
  template:
    spec:
      containers:
        - name: redis
          resources:
            requests:
              cpu: "75m"
              memory: "96Mi"
            limits:
              cpu: "250m"
              memory: "384Mi"
