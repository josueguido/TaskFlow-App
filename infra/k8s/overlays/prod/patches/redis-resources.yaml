apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: redis
  namespace: taskflow-app
spec:
  template:
    spec:
      containers:
        - name: redis
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
