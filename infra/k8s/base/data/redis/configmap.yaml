apiVersion: v1
kind: ConfigMap
metadata:
  name: redis-config
  namespace: taskflow-app
data:
  redis.conf: |
    bind 0.0.0.0
    port 6379
    protected-mode yes
    appendonly yes
    appendfsync everysec
    save 900 1
    save 300 10
    save 60 10000
    dir /data
    maxmemory-policy allkeys-lru

